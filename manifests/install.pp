class saltstack::install inherits saltstack {

  # if($saltstack::manage_package)
  # {
  #   # package here, for example:
  #   #package { $saltstack::params::package_name:
  #   #  ensure => $saltstack::package_ensure,
  #   #}
  # }

}
